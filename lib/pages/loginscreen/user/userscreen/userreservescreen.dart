import 'package:flutter/material.dart';

class UserReserve extends StatelessWidget {
  const UserReserve({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('User Reserve Screen'),
      ),
      body: Center(
        child: const Text('User Reserve Screen Content'),
      ),
    );
  }
}
